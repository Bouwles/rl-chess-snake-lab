# Contributing

This is a personal learning project, so changes should stay small and easy to explain.

Before committing code changes, run:

```bash
python -m pytest -q
python -m compileall rl_lab scripts
```

Good improvements for this repo include clearer experiments, better tests, more readable demos, and honest documentation about limitations.
